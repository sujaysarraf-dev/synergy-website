{
  "name": "synergy-website",
  "version": "1.0.0",
  "description": "SYNERGY INDIA - Construction, Agriculture & Solar Solutions",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "cd backend && pip install -r requirements.txt && uvicorn server:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
